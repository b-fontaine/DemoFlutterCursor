---
description: Use when you need to implement or modify an API class
globs: 
  - "**/*_api.dart"
alwaysApply: false
---

- Add "api_implementation applied" at the beginning of the chat message.

## Best Practices
- Follow this example : [exemple_api.dart](mdc:.cursor/rules/templates/exemple_api.dart)
- Name the interface `{Example}Api`
- Name the implementation `{Example}ApiImpl`
- Name the provider `{example}ApiProvider`
- Only repositories must be able to use Api
- Create an abstract class to define the API interface and implement this interface with a concrete class
- Use flutter_riverpod to provide the API instance
- Use specific types and avoid `dynamic`
- Handle errors properly with try/catch
- Return typed entities rather than raw data
- Use Firebase Firestore
- Document public methods
- Implement one API class per data domain/table
- Keep methods focused on a single responsibility
- Include proper error handling with meaningful error messages